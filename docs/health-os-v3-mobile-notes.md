# Mobile notes

Target visual width: 390–430 px. Search input is forced to 16px to avoid iOS focus zoom. The floating dock keeps safe-area clearance. Filter sheet uses viewport-relative height and the existing disclosure element, avoiding new client state or query logic.
