import Link from "next/link";
import { notFound } from "next/navigation";
import { SiteHeader } from "@/components/SiteHeader";
import { SiteFooter } from "@/components/SiteFooter";
import { cityDetails, professions } from "@/lib/data";

export default async function CityPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const city=cityDetails[slug];if(!city)notFound();return <><SiteHeader/><main><section className="city-hero"><div className="shell"><p className="eyebrow">{city.region}</p><h1>Saúde em {city.name}.</h1><p>{city.intro}</p><Link href={`/buscar?cidade=${encodeURIComponent(city.name)}`}>Buscar profissionais em {city.name} →</Link></div></section><section className="shell content-section"><div className="city-summary"><div><small>GUIA LOCAL</small><strong>Profissionais</strong><span>Cadastros em validação</span></div><div><small>SERVIÇOS</small><strong>Clínicas e empresas</strong><span>Diretório em formação</span></div><div><small>CONTEÚDO</small><strong>Notícias da cidade</strong><span>Publicação editorial</span></div></div><div className="content-title"><div><p className="eyebrow">Encontre cuidado</p><h2>Áreas mais procuradas</h2></div></div><div className="city-professions">{professions.map(item=><Link key={item} href={`/buscar?cidade=${encodeURIComponent(city.name)}&profissao=${encodeURIComponent(item)}`}><strong>{item}</strong><span>Ver na cidade →</span></Link>)}</div><div className="local-content"><div><p className="eyebrow">Conteúdo local</p><h2>Informação conectada à realidade de {city.name}.</h2><p>Esta área reunirá notícias, campanhas, eventos, entrevistas e orientações relacionadas à saúde no município.</p></div><aside><small>QUER PARTICIPAR?</small><strong>Profissionais e empresas de {city.name}</strong><p>Solicite inclusão no guia ou envie uma pauta para a equipe editorial.</p><Link href="/anuncie">Falar com o Guia Saúde →</Link></aside></div></section></main><SiteFooter/></>}
